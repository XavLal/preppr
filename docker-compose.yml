services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      PORT: "3000"
      DATA_DIR: /data
      CLIENT_DIST: /app/static
    volumes:
      - mealplanner-data:/data

volumes:
  mealplanner-data: {}
